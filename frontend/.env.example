# Copy to `.env` (or set in your shell) before building.
# Absolute URL used by the built frontend to reach the API.
# Production default (same origin): leave empty or unset to use "/api".
VITE_API_URL=
